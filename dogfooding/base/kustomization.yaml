# Copyright Contributors to the KubeOpenCode project
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeopencode-dogfooding

resources:
  - namespace.yaml
  - rbac.yaml
  - secrets.yaml
  - kubeopencodeconfig.yaml
  - agent-dogfooding.yaml

# Note: Agent capabilities are defined by .claude/skills/ in the dogfooding repo.
# See ../scheduled/ for CronJob configuration.
# See ../slack/ for Slack gateway configuration.
